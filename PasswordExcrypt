
#include<stdio.h>
#include<string.h>
int main()
{
 char pass[50];
 int i, len=0;
 
 printf("Enter your password:");
 scanf("%s",pass);
 
 len = strlen(pass);
 printf("Your encrypted password is: ");
 
 for(i=0; i<len; i++)
  printf("%c",pass[i]+3); 

 return 0;
}